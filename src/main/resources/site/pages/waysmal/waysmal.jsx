import React from 'react';
import Regions from '@enonic/react-components/Regions';

export default (props) => {
    return (
      <div className="waysmal-page">
          <Regions {...props} />
      </div>
    );
  };